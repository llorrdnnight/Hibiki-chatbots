Chatbot
Install adaconda
Conda create -n chatbot  python=3.6
Conda activate chatbot
pip install nltk 			// Natural Language Toolkit propably  you need to download it to in chatbot.py add "nltk.download("all")" if it gives errors 
 pip install numpy 			// support for large, multi-dimensional arrays and matrices
pip install flask 			// micro web framework
pip install tflearn 			// Deep learning library
pip install tensorflow			// open-source software library for machine learning
pip install mysql-connector-python 	//connecting to database

Xampp
database='chatbot',
user='webuser',
password='Lab2020'
